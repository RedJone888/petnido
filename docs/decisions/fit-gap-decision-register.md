# T04 Fit-Gap 决策登记

| 编号 | 领域 | 决策 | 理由 | 状态 |
|---|---|---|---|---|
| D01 | 新建需求 | `/needs/create` 引导流程是正式方向，接入新契约；旧后台表单仅短期回退 | 新 UI 更接近三模式计划，但当前是假发布 | ACCEPTED |
| D02 | 公开路由 | 正式采用 `/needs`、`/services`、`/providers/[id]`；旧 `/public/*` 做临时重定向 | URL 更清晰，游客语义无需 public 前缀 | ACCEPTED |
| D03 | 首次登录 | 新用户完成 profile→intent；老用户直接 `/dashboard` | 符合确认后的行为轨迹 | ACCEPTED |
| D04 | 宠物/地点默认 | 在流程对应步骤读取档案；发布成功后自动 upsert | 不改变用户填写顺序 | ACCEPTED |
| D05 | 服务默认 | 在对应步骤读取服务档案；成功后回写地点和币种，允许单次覆盖 | 默认值与本次服务分离 | ACCEPTED |
| D06 | 过期 | `endDate <= now` 即过期；公开查询排除，收藏保留 | 单一且可计算 | ACCEPTED |
| D07 | 位置 | 仅持久化 lat/lon 和可选非精确 regionLabel | 不采集楼号、楼层、房号等精确地址 | ACCEPTED |
| D08 | 容量 | 仅 BOARDING 服务限制；按成功预约的宠物数量累计 | 其他模式不适用家庭容量 | ACCEPTED |
| D09 | 确认 | 点击应聘/预约先建 PENDING + 会话；对方确认后才成功 | 与产品描述一致 | ACCEPTED |
| D10 | 邮件 | 发布成功后若未开邮件通知则询问；邮件不含健康、地址或消息正文 | 及时提醒同时减少敏感外发 | ACCEPTED |
| D11 | 健康数据 | 普通受保护照护数据，不作为公开信息或日志字段 | 不是最高敏感等级，但仍需访问控制 | ACCEPTED |
| D12 | 现有后台 | 复用布局、卡片和基础表单组件；重建 mock 数据层和状态逻辑 | 保留视觉资产，避免继承错误事实模型 | ACCEPTED |

## 退出标准

旧路径或旧模型只有在新路径通过集成测试、数据回填对账、回滚演练并稳定一个发布周期后才可删除。

## 自验收

- [x] 已纳入用户明确确认的 4 项规则和其余建议方案。
- [x] 所有主要冲突都有可执行决策，不用“待定”掩盖实现选择。
- [x] 每项可追溯到页面、API 或数据迁移工作。

**T04 决策登记验收结论：通过。**
