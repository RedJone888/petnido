# 阶段 2：档案、设置与登录续接检查报告

> 状态：功能检查点通过，阶段 2 尚未关闭  
> 日期：2026-08-04  
> 分支：`codex/petnido-v2-development`

## 本检查点完成内容

- 首次用户的“头像昵称 → 初始意图 → 服务经验资料”服务端状态流。
- 非首次用户直接进入个人中心，不增加消息提示步骤。
- 安全的登录回跳：保留 Dashboard 子路径与查询参数，拒绝外部重定向。
- 签名 PendingAction 框架：收藏、咨询、应聘、预约动作具有类型、目标、随机 nonce、过期时间和 HMAC 签名；未开放动作不会创建业务数据。
- 个人资料、宠物档案、地图位置、服务档案和邮件通知偏好的本人 API 与管理页面。
- 常用位置只写入经纬度、非精确区域标签与展示精度；不新增精确地址写入。
- 删除默认位置时自动选择剩余位置作为默认，并同步服务档案默认位置。
- 服务提供身份与“当前是否接单”分离；暂停接单不会移除服务提供身份。
- 旧服务资料编辑入口不再写入 `baseAreaRaw`，改为进入新的服务档案设置。
- Dashboard 通用错误页、不可枚举的不存在页和 Edge 安全中间件认证配置。

## 自行验收结果

| 验收项 | 结果 |
|---|---|
| TypeScript | 通过 |
| ESLint | 通过；仅保留路线图已登记的旧代码 warning |
| 单元测试 | 7 个文件、29 项通过 |
| 集成测试 | 3 个文件、15 项通过 |
| 桌面/移动 E2E | 10 项通过 |
| 三语言键一致性 | 144 个叶子键一致 |
| 位置隐私扫描 | 通过 |
| Prisma production schema validate | 通过 |
| Production build | 通过，40 个 App Router 页面；Edge `bcrypt` 警告已消除 |
| 浏览器人工冒烟 | 登录弹窗桌面/390px、关闭与重开、Dashboard 精确回跳、无效 PendingAction 均通过 |

新增的关键回归用例覆盖：

- 首次引导状态只能按服务器顺序推进。
- 服务资料初始化具有幂等性。
- 宠物和位置跨用户修改被拒绝。
- 默认位置唯一、删除后自动接替。
- 不能把其他用户的位置设为服务默认位置。
- 接单总开关持久化且不取消服务提供身份。
- 邮件通知偏好按用户隔离。
- PendingAction 篡改、过期和外部回跳均失败关闭。
- `/dashboard/settings?section=provider` 登录后仍返回完整原路径。

## 阶段 2 关闭前仍需完成

1. 在应用生产模型对应的隔离 PostgreSQL 测试库应用 expand migration，完成已登录设置页的桌面、390px、键盘和 200% 缩放实测。当前没有对 `.env.local` 指向的数据库执行 migration。
2. 将新增设置页和 PendingAction 页面接入三语言资源；目前三语言资源结构与键门禁正常，但新增业务正文仍以日文为主。
3. 补齐头像文件上传及附件归属/清理；当前个人资料可保存头像 URL，不能把它当作最终上传体验。
4. 将位置输入升级为地图选点体验；当前持久化合同正确，但管理页主要使用经纬度输入。
5. 公开服务查询在阶段 4 接入时必须同时检查 `ServiceProfile.isAccepting`，否则接单总开关只完成了事实来源，没有完成公开市场消费端。
6. 发布成功后的邮箱通知询问在阶段 3 的真实发布完成页接入；当前只建立偏好数据和设置入口。

## 数据库结论

- 新增 expand migration：`20260804040000_service_profile_accepting`。
- 未执行生产 migration、未修改生产数据。
- 迁移策略保持 expand-only；现有服务档案默认保持接单，避免升级后意外全部下线。
